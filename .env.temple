# Copy this file to `.env` and fill in real values before local SMTP testing.

# Required for email sending
SENDER=
RECEIVER=
SENDER_PASSWORD=

# Optional SMTP overrides
SMTP_SERVER=smtp.qq.com
SMTP_PORT=465

# Optional debug logging
DEBUG=true
